[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny_cext"
version = "0.1.0"
description = "CPython C-extension (static FFI) binding for the tiny C library."
requires-python = ">=3.8"
