--- a/c/include/tiny.h
+++ b/c/include/tiny.h
@@ -10,6 +10,9 @@
 /* Returns a - b. */
 int tiny_diff(int a, int b);
 
+/* Returns max(a, b). Added in this version; no binding wraps it yet. */
+int tiny_max(int a, int b);
+
 #ifdef TINY_DEV
 /* Returns a * k. Added in the "dev" version (TINY_2.0); absent from "stable"
    (TINY_1.0). A consumer built against dev that calls tiny_scale fails against
--- a/c/src/tiny.c
+++ b/c/src/tiny.c
@@ -10,6 +10,10 @@
     return a - b;
 }
 
+int tiny_max(int a, int b) {
+    return a > b ? a : b;
+}
+
 #ifdef TINY_DEV
 int tiny_scale(int a, int k) {
     return a * k;
