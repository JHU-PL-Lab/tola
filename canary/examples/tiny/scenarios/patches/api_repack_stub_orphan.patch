--- a/ocaml/tiny_raw.mli
+++ b/ocaml/tiny_raw.mli
@@ -4,3 +4,6 @@
 external sum        : int -> int -> int = "caml_tiny_sum"
 external diff       : int -> int -> int = "caml_tiny_diff"
 external get_offset : unit -> int       = "caml_tiny_get_offset"
+
+(** Stub-side orphan: an alias for tiny_sum exposed at s3 but never repacked into Tiny.mli (s4). *)
+external alias_sum  : int -> int -> int = "caml_tiny_alias_sum"
--- a/ocaml/tiny_raw.ml
+++ b/ocaml/tiny_raw.ml
@@ -1,3 +1,4 @@
 external sum        : int -> int -> int = "caml_tiny_sum"
 external diff       : int -> int -> int = "caml_tiny_diff"
 external get_offset : unit -> int       = "caml_tiny_get_offset"
+external alias_sum  : int -> int -> int = "caml_tiny_alias_sum"
--- a/ocaml/tiny_stubs.c
+++ b/ocaml/tiny_stubs.c
@@ -17,3 +17,8 @@
     CAMLparam1(unit);
     CAMLreturn(Val_int(tiny_offset));
 }
+
+CAMLprim value caml_tiny_alias_sum(value a, value b) {
+    CAMLparam2(a, b);
+    CAMLreturn(Val_int(tiny_sum(Int_val(a), Int_val(b))));
+}
