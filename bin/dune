(include_subdirs unqualified)

(executables
 (names dd arith_fix enum1 enum2 text text_example pm pmm try_fix)
 (libraries
  fix
  fmt
  cmdliner
  langs
  parser
  feat
  std
  ocamlgraph
  interp
  ainterp
  example)
 (preprocess
  (pps ppx_deriving.show))
 (promote (until-clean)))

(env
 (dev
  (flags
   (:standard -w -32))))
