(include_subdirs unqualified)

(executables
 (names cmake_see)
 (modules cmake_see)
 (modes byte)
 (libraries
  fix
  fmt
  sexplib
  cmdliner
  parser
  std
  ocamlgraph
  interp
  ainterp
  versioned_maps
  example
  langs)
 (preprocess
  (pps ppx_deriving.show))
 (promote (until-clean)))

; (executables
;  (names
;   lt
;   ltpm
;   lt_example
;   lts
;   ltspm
;   ltmp
;   ltmppm
;   md
;   mdpm
;   shell
;   dump_mm
;   dump_mp
;   dd
;   radix
;   superego
;   arith_fix
;   try_fix)
;  (libraries
;   fix
;   fmt
;   sexplib
;   cmdliner
;   parser
;   std
;   ocamlgraph
;   interp
;   ainterp
;   versioned_maps
;   example
;   langs)
;  (preprocess
;   (pps ppx_deriving.show))
;  (promote (until-clean)))

(env
 (dev
  (flags
   (:standard -w -32))))
