(include_subdirs unqualified)

(executables
 (names dd arith_fix enum1 enum2 text stext text_example pm spm try_fix)
 (libraries
  fix
  fmt
  cmdliner
  parser
  feat
  std
  ocamlgraph
  interp
  ainterp
  example)
 (preprocess
  (pps ppx_deriving.show))
 (promote (until-clean)))

(env
 (dev
  (flags
   (:standard -w -32))))
