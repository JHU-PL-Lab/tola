# Canary status — 2026-05-06

## Active TODOs (numbered, stable)

- **#19** — LLVM C-symbol cross-version check (OCaml mismatch done, C-symbol level not yet)
- **#40** — Replace fake `install_lib` (`cp`) with real `cmake --install`

Backlog: ~20 items in `doc/canary/backlog.md`.

## api_surface — done through Step 2

- ✅ Step 1 — `inspect_cmd` for native/ocaml/python/mli/stub kinds
- ✅ Step 2 — watchlists per project, `inspect` field on `script_spec`, install-step + probe-step inspects
- ✅ `inspect-diff`, `canary compat`, `canary verify`, `Expect_compat_failure`
- ⏳ Step 3 — `inspect-sync` into committed `artifact_inspect.json` (deferred)

## Diagram system

- File split: `canary_action.ml` (2628→1211) + new `canary_diagram.ml` (2280) + trimmed `canary.ml` (658)
- `mermaid_of_action_rule_schema` moved from `canary.ml` to `canary_diagram.ml` — all diagram code in one file
- `-run` path: renamed from `_run`, GitHub Pages compatible
- `docs/` auto-publish with `.ok`/binary filtering
- `artifact_name` in project specs, persisted to `run_state.json`
- Producer `[N]` IDs on artifact labels in focused views
- Invariant checker: coverage check works, connectivity has false positives on BFS
- Spin-off TODO: model-first diagram architecture

## api_surface layer types — all six levels represented

| Layer | Data extraction                              | Compat variant                 | Project wired         |
| ----- | -------------------------------------------- | ------------------------------ | --------------------- |
| L1a   | `inspect_native.py` (`nm -D`) ✅              | `Native_lib` / `C_stub` ✅      | z3, llvm ✅            |
| L1b   | `inspect_native.py` (`versioned_req`) ✅      | `Versioned_symbols` ✅          | — needs concrete case |
| L2    | —                                            | `type_watchlist` placeholder   | —                     |
| L3    | `inspect_binding.py` / `inspect_python.py` ✅ | `Ocaml_mli` / `Python_attrs` ✅ | z3/stable, llvm/19 ✅  |
| L4    | `inspect_native.py --elf` (`readelf -d`) ✅   | `Abi_surface` ✅                | — needs concrete case |
| L5    | —                                            | —                              | research territory    |

## Known gaps

- macOS support (three phases, partially scaffolded)
- Retire legacy `project_config` plumbing (dead code, intentional)
- Old `src/binding/` inspectors to migrate (~1880 lines)
- Source-artifact inspect (Git SHA, header count)
- PyTorch as multi-PM target (plan in `new_project.md`)

## Naming

- ✅ `summary→inspect` rename DONE — scripts, functions, fields, step tags, JSON files
- ✅ `interface→surface` rename DONE — `api_surface.md`, "surface contract"
- `api_source`, `binding_api`, `native_api` types unchanged (describe API structure)
