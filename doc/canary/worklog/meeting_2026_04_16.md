## Brought LLVM to Canary

See [doc/canary/llvm_build_note.md].
  - reviewed old snippets and recent update to configure a working ~7min building.
  - use `sccache` which is modern alternative to `ccache`.

Review current opam llvm package and make an opam `llvm.dev`, see [canary/templates/opam-local-repo/packages]
  - it currently bypasses `conf-llvm` package since it builds from source.

Canary uses the example that only the dev version can pass [canary/examples/llvm/llvm_example_dev.ml] with new `Opcode.UncondBr`

```
[2026-04-16 12:41:14.139] probe_binding              cmd  (eval $(opam env)
ocamlfind ocamlopt -package llvm -linkpkg /home/red/code/research/tola/canary/examples/llvm/llvm_example_dev.ml \
  -o /home/red/code/research/tola/_out/canary/_local/llvm/19/probe_binding/llvm_example
/home/red/code/research/tola/_out/canary/_local/llvm/19/probe_binding/llvm_example 2>&1 | tee /home/red/code/research/tola/_out/canary/_local/llvm/19/probe_binding/probe.log)
File "/home/red/code/research/tola/canary/examples/llvm/llvm_example_dev.ml", line 17, characters 33-48:
17 |   assert (instr_opcode br_inst = Opcode.UncondBr);
                                      ^^^^^^^^^^^^^^^
Error: Unbound constructor Opcode.UncondBr
```

The shape of both z3 and llvm canary testing is working, the build-cache reuse for opam packages with source is broken.
  - It's only for local developping.
  - Their `opam` file takes a `BUILD_PATH` which passed to `cmake` inside `opam`. It points to a used path, so every building occurs just once. I modified(unified) the paths from local `_out/canary/_local` to global, but it may have some legecy code. `conf-llvm` is not tested which takes another template `opam` file. For z3's case, there is just one template `opam` file.

The majority of llvm is at least working, for canary, the immediate todo are:
  - Expectation judging from interfaces
  - Project spec templating which can be easily applied for more projects

## Weekend fun

pl4ai: ecosem, what are canonical components for pl, what are the contracts between them, and can ai boostrap for itself.
ai4pl: yelu, how can a new lang be invented with legacy pl infra