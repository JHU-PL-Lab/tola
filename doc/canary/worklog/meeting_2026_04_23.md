## Deploy on GH CI

- Worked for Z3, LLVM and Sqlite
- Generated from the same set of project specificaion and action deriver.
- For bottleneck, make a working mold+sccache toolchains. It also works locally.

## Symbol-to-versioning Check

- Migrate symbol detection as an per-artifact analysis. Used to have working function and now move the action canary.
- For compatibility check, currently use a handwritten watchlist, especially `missing`
  - it uses to predicate action expection
- to-do: move the previous linker/loader path
- to-do: migrate project c-api analysis

## Python binding

- Covered Z3, LLVM, sqlite. Tested locally, not GH CI tests yet.
- Working on a general functions (I have two instances before making an abstraction) of canary to iterate on bindings

- bring in mac

## Misc
- cleanup and passes of code audio
- start to extend the projects into a dozen, selected from opam survey
torch