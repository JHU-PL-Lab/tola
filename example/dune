(include_subdirs unqualified)

(library
 (name example)
 ;  (public_name tola.example)
 (libraries fmt langs)
 (preprocess
  (pps ppx_deriving.eq ppx_deriving.show)))

(env
 (dev
  (flags
   (:standard -w -32))))
